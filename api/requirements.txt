# Runtime dependencies for the FastAPI backend deployed as Vercel
# serverless functions.  Dev/test dependencies (pytest, black, etc.)
# are intentionally excluded to keep cold-start fast.

fastapi>=0.110.1
python-dotenv>=1.0.1
motor>=3.3.1
pymongo>=4.6.3
pydantic>=2.6.4
email-validator>=2.2.0
httpx>=0.27.0
stripe>=9.0.0
python-multipart>=0.0.9
